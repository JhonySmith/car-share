import styled from 'styled-components';

export const MapSection = styled.section`
  position: relative;
  width: 1662px;
  margin-top: 25px;

  border-radius: 10px;
  overflow: hidden;
`;

export const MapBlock = styled.iframe`
  border: none;
`;
